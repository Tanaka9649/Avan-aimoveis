"use client";
import { useActionState } from "react";
import { saveProposal,saveSale } from "@/app/painel/propostas/actions";
import { DateInputField } from "./date-time-fields";
type Choice={id:string;label:string};
const Feedback=({state}:{state:{ok:boolean;message:string}})=>state.message?<p className={state.ok?"form-success-inline":"admin-form-error"}>{state.message}</p>:null;
export function ProposalForm({deals,properties}:{deals:Choice[];properties:Choice[]}){const[state,action,pending]=useActionState(saveProposal,{ok:false,message:""});return <form action={action} className="entity-form operational-form"><label>Oportunidade<select name="dealId" required defaultValue=""><option value="">Selecione</option>{deals.map(x=><option key={x.id} value={x.id}>{x.label}</option>)}</select></label><label>Imóvel<select name="propertyId" required defaultValue=""><option value="">Selecione</option>{properties.map(x=><option key={x.id} value={x.id}>{x.label}</option>)}</select></label><label>Valor anunciado<input name="advertised" type="number" min="0.01" step="0.01" required/></label><label>Valor ofertado<input name="amount" type="number" min="0.01" step="0.01" required/></label><label>Contraproposta<input name="counter" type="number" min="0.01" step="0.01"/></label><DateInputField name="validUntil" label="Validade da proposta" hint="Opcional. A proposta expira no fim deste dia."/><label>Status<select name="status" defaultValue="enviada"><option value="enviada">Enviada</option><option value="em_negociacao">Em negociação</option><option value="contraproposta">Contraproposta</option><option value="aceita">Aceita</option><option value="recusada">Recusada</option><option value="expirada">Expirada</option></select></label><label className="wide">Observações<textarea name="notes" rows={3}/></label><Feedback state={state}/><button className="admin-button primary" disabled={pending}>{pending?"Salvando…":"Salvar proposta"}</button></form>}
export function SaleForm({deals,properties,proposals}:{deals:Choice[];properties:Choice[];proposals:Choice[]}){const[state,action,pending]=useActionState(saveSale,{ok:false,message:""});return <form action={action} className="entity-form operational-form"><label>Oportunidade<select name="dealId" required defaultValue=""><option value="">Selecione</option>{deals.map(x=><option key={x.id} value={x.id}>{x.label}</option>)}</select></label><label>Imóvel<select name="propertyId" required defaultValue=""><option value="">Selecione</option>{properties.map(x=><option key={x.id} value={x.id}>{x.label}</option>)}</select></label><label>Proposta<select name="proposalId" defaultValue=""><option value="">Sem proposta vinculada</option>{proposals.map(x=><option key={x.id} value={x.id}>{x.label}</option>)}</select></label><label>Valor anunciado<input name="advertised" type="number" min="0.01" step="0.01" required/></label><label>Valor final<input name="amount" type="number" min="0.01" step="0.01" required/></label><label>Comissão (%)<input name="commissionPercent" type="number" min="0" max="100" step="0.01" required/></label><DateInputField name="soldAt" label="Data da venda" required/><label className="wide">Observações<textarea name="notes" rows={3}/></label><Feedback state={state}/><button className="admin-button primary" disabled={pending}>{pending?"Concluindo…":"Concluir venda"}</button></form>}
