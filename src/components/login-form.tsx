"use client";
import { useState } from "react";
import { Loader2, LockKeyhole } from "lucide-react";
import { useRouter } from "next/navigation";
export function LoginForm(){const router=useRouter();const[loading,setLoading]=useState(false);const[error,setError]=useState("");async function submit(formData:FormData){setLoading(true);setError("");const response=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(Object.fromEntries(formData))});const data=await response.json();if(response.ok){router.push("/painel");router.refresh();return}setError(data.error);setLoading(false)}return <form className="login-form" action={submit}><div className="login-icon"><LockKeyhole/></div><div><span className="eyebrow">Área restrita</span><h1>Entre no painel</h1><p>Use as credenciais da sua equipe.</p></div><label>E-mail<input name="email" type="email" autoComplete="email" required/></label><label>Senha<input name="password" type="password" autoComplete="current-password" minLength={8} required/></label>{error&&<p className="login-error" role="alert">{error}</p>}<button className="button button-accent" disabled={loading}>{loading?<><Loader2 className="spin"/>Entrando...</>:"Entrar com segurança"}</button></form>}
