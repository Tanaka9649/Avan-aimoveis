"use client";
import Image from "next/image";import Link from "next/link";import {Bath,BedDouble,Car,Heart,MapPin,MoveUpRight} from "lucide-react";import type {Property} from "@/data/properties";import {formatArea,formatMoney} from "@/lib/format";import {useSyncExternalStore} from "react";import {readFavorites} from "@/lib/favorites";
const FAVORITES_KEY="avan:favorites";
export function PropertyCard({property}:{property:Property}){const saved=useSyncExternalStore(callback=>{window.addEventListener("favorites:changed",callback);return()=>window.removeEventListener("favorites:changed",callback)},()=>readFavorites().includes(property.id),()=>false);const toggle=()=>{const current=readFavorites();const next=current.includes(property.id)?current.filter(id=>id!==property.id):[...current,property.id];localStorage.setItem(FAVORITES_KEY,JSON.stringify(next));window.dispatchEvent(new Event("favorites:changed"));};return <article className="property-card"><div className="property-image"><Image src={property.image} alt={`Fachada e ambientes de ${property.title}`} fill sizes="(max-width: 760px) 100vw, 33vw" unoptimized={property.image.startsWith("/api/")}/><button className={`favorite ${saved?"saved":""}`} onClick={toggle} aria-label={saved?"Remover dos favoritos":"Adicionar aos favoritos"}><Heart size={19} fill={saved?"currentColor":"none"}/></button><span className="property-code">{property.code}</span></div><div className="property-card-body"><div className="eyebrow"><MapPin size={14}/>{property.neighborhood} · {property.city}</div><h3><Link href={`/imoveis/${property.slug}`}>{property.title}</Link></h3><div className="property-specs"><span><BedDouble/> {property.bedrooms}</span><span><Bath/> {property.bathrooms}</span><span><Car/> {property.parkingSpaces}</span><span>{formatArea(property.area)}</span></div><div className="property-price"><strong>{formatMoney(property.priceCents)}</strong><Link href={`/imoveis/${property.slug}`} aria-label={`Ver ${property.title}`}><MoveUpRight/></Link></div></div></article>}
