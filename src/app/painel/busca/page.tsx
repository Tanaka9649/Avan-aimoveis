import Link from "next/link";import { and,ilike,or } from "drizzle-orm";import { Building2,KanbanSquare,Search,Users } from "lucide-react";import { getDb } from "@/db";import { clients,deals,properties } from "@/db/schema";import { clientScope } from "@/lib/access";import { requireUser } from "@/lib/auth";import { EmptyState,PageHeader,SectionCard,StatusBadge } from "@/components/admin-ui";
export default async function GlobalSearchPage({searchParams}:{searchParams:Promise<{q?:string}>}){const user=await requireUser();const q=(await searchParams).q?.trim()||"";const db=getDb();const[clientRows,propertyRows,dealRows]=q?await Promise.all([db.select({id:clients.id,name:clients.name,phone:clients.phone,email:clients.email}).from(clients).where(and(clientScope(user),or(ilike(clients.name,`%${q}%`),ilike(clients.phone,`%${q}%`),ilike(clients.email,`%${q}%`)))).limit(12),db.select({id:properties.id,title:properties.title,code:properties.code,status:properties.status}).from(properties).where(or(ilike(properties.title,`%${q}%`),ilike(properties.code,`%${q}%`),ilike(properties.city,`%${q}%`),ilike(properties.neighborhood,`%${q}%`))).limit(12),db.select({id:deals.id,title:deals.title}).from(deals).where(ilike(deals.title,`%${q}%`)).limit(12)]):[[],[],[]];const total=clientRows.length+propertyRows.length+dealRows.length;return <div className="admin-content"><PageHeader eyebrow="Pesquisa" title="Busca global" description={q?`${total} resultado(s) para “${q}”.`:"Pesquise clientes, contatos, imóveis, códigos e oportunidades."}/>{!q?<EmptyState icon={Search} title="Digite o que procura" description="Use a busca no topo ou o atalho Ctrl/Cmd + K."/>:<div className="global-search-results"><SectionCard title="Clientes" description={`${clientRows.length} resultado(s)`}>{clientRows.length?clientRows.map(x=><Link key={x.id} href={`/painel/clientes/${x.id}`}><Users/><div><strong>{x.name}</strong><small>{x.phone} · {x.email||"Sem e-mail"}</small></div></Link>):<p className="muted-copy">Nenhum cliente encontrado.</p>}</SectionCard><SectionCard title="Imóveis" description={`${propertyRows.length} resultado(s)`}>{propertyRows.length?propertyRows.map(x=><Link key={x.id} href={`/painel/imoveis/${x.id}`}><Building2/><div><strong>{x.title}</strong><small>{x.code}</small></div><StatusBadge value={x.status}/></Link>):<p className="muted-copy">Nenhum imóvel encontrado.</p>}</SectionCard><SectionCard title="Oportunidades" description={`${dealRows.length} resultado(s)`}>{dealRows.length?dealRows.map(x=><Link key={x.id} href={`/painel/crm/${x.id}`}><KanbanSquare/><strong>{x.title}</strong></Link>):<p className="muted-copy">Nenhuma oportunidade encontrada.</p>}</SectionCard></div>}</div>}
