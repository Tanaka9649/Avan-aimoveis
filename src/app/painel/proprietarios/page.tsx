import Link from "next/link";import { count,desc,eq } from "drizzle-orm";import { Contact,Plus } from "lucide-react";import { requireModule } from "@/lib/access";import { getDb } from "@/db";import { owners,propertyOwners } from "@/db/schema";import { EmptyState,PageHeader,SectionCard } from "@/components/admin-ui";import { OwnerForm } from "@/components/owner-form";
export default async function OwnersPage(){await requireModule("proprietarios");const rows=await getDb().select({id:owners.id,name:owners.name,phone:owners.phone,email:owners.email,createdAt:owners.createdAt,properties:count(propertyOwners.propertyId)}).from(owners).leftJoin(propertyOwners,eq(propertyOwners.ownerId,owners.id)).groupBy(owners.id).orderBy(desc(owners.createdAt)).limit(100);return <div className="admin-content"><PageHeader eyebrow="Captação" title="Proprietários" description="Cadastre contatos e acompanhe os imóveis vinculados." action={<a className="admin-button primary" href="#novo-proprietario"><Plus/> Novo proprietário</a>}/><SectionCard className="operation-composer" title="Novo proprietário" description="Dados protegidos e restritos à equipe."><div id="novo-proprietario"><OwnerForm/></div></SectionCard>{rows.length?<section className="admin-card table-card"><div className="table-toolbar"><div><strong>{rows.length} proprietários</strong><span>Cadastros mais recentes</span></div></div><div className="table-scroll"><table><thead><tr><th>Nome</th><th>Telefone</th><th>E-mail</th><th>Imóveis</th><th></th></tr></thead><tbody>{rows.map(o=><tr key={o.id}><td><Link href={`/painel/proprietarios/${o.id}`}><strong>{o.name}</strong></Link></td><td>{o.phone}</td><td>{o.email||"—"}</td><td>{o.properties}</td><td><Link className="admin-button secondary" href={`/painel/proprietarios/${o.id}`}>Abrir</Link></td></tr>)}</tbody></table></div></section>:<EmptyState icon={Contact} title="Nenhum proprietário cadastrado" description="Cadastre o primeiro proprietário para vinculá-lo aos imóveis."/>}</div>;}
