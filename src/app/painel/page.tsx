import { and, count, eq, gte, sum } from "drizzle-orm";
import { ArrowDownRight, ArrowUpRight, Building2, CalendarCheck, CircleDollarSign, Handshake, Users } from "lucide-react";
import { getDb } from "@/db";
import { clients, deals, properties, sales, visits } from "@/db/schema";
import { formatMoney } from "@/lib/format";
export default async function DashboardPage(){const db=getDb();const monthStart=new Date();monthStart.setDate(1);monthStart.setHours(0,0,0,0);const [[clientTotal],[newClients],[activeProperties],[openDeals],[visitTotal],[salesTotal]]=await Promise.all([db.select({value:count()}).from(clients),db.select({value:count()}).from(clients).where(gte(clients.createdAt,monthStart)),db.select({value:count()}).from(properties).where(eq(properties.status,"disponivel")),db.select({value:count()}).from(deals),db.select({value:count()}).from(visits).where(and(gte(visits.scheduledAt,monthStart),eq(visits.status,"agendada"))),db.select({value:sum(sales.amountCents)}).from(sales).where(gte(sales.soldAt,monthStart))]);const metrics=[{label:"Clientes",value:clientTotal.value.toString(),delta:`+${newClients.value} este mês`,Icon:Users,up:true},{label:"Imóveis ativos",value:activeProperties.value.toString(),delta:"portfólio disponível",Icon:Building2,up:true},{label:"Negócios no funil",value:openDeals.value.toString(),delta:"em acompanhamento",Icon:Handshake,up:true},{label:"Visitas agendadas",value:visitTotal.value.toString(),delta:"no mês atual",Icon:CalendarCheck,up:true},{label:"Vendas no mês",value:formatMoney(Number(salesTotal.value||0)),delta:"valor realizado",Icon:CircleDollarSign,up:true}];return <div className="admin-content"><div className="admin-page-title"><div><span>Visão geral</span><h1>Bom trabalho. Aqui está o pulso da operação.</h1></div><button className="admin-primary">+ Novo negócio</button></div><div className="metric-grid">{metrics.map(({label,value,delta,Icon,up})=><article key={label}><div><span>{label}</span><Icon/></div><strong>{value}</strong><small className={up?"positive":"negative"}>{up?<ArrowUpRight/>:<ArrowDownRight/>}{delta}</small></article>)}</div><div className="dashboard-grid"><section className="admin-card pipeline"><div className="card-title"><div><span>Funil comercial</span><h2>Distribuição por etapa</h2></div><button>Ver CRM</button></div><div className="bars"><div><label>Novos leads <b>12</b></label><span><i style={{width:"82%"}}/></span></div><div><label>Em contato <b>8</b></label><span><i style={{width:"58%"}}/></span></div><div><label>Visita <b>5</b></label><span><i style={{width:"40%"}}/></span></div><div><label>Proposta <b>3</b></label><span><i style={{width:"27%"}}/></span></div></div></section><section className="admin-card agenda"><div className="card-title"><div><span>Hoje</span><h2>Próximos compromissos</h2></div></div><div className="agenda-item"><time>10:30</time><div><strong>Visita · AV-1042</strong><p>Marina Alves · Jardins</p></div><span className="status scheduled">Agendada</span></div><div className="agenda-item"><time>15:00</time><div><strong>Retorno de proposta</strong><p>Gustavo Lima · Cobertura</p></div><span className="status pending">Pendente</span></div><div className="agenda-item"><time>17:30</time><div><strong>Avaliação de captação</strong><p>Alto de Pinheiros</p></div><span className="status scheduled">Agendada</span></div></section></div></div>}
