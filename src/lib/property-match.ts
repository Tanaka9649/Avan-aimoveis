type ClientPreference={budgetMinCents:number|null;budgetMaxCents:number|null;desiredTypes:string[];desiredRegions:string[];minBedrooms:number|null;minBathrooms:number|null;minParkingSpaces:number|null;desiredFeatures:string[]};
type PropertyProfile={priceCents:number;type:string;neighborhood:string;city:string;bedrooms:number;bathrooms:number;parkingSpaces:number;features:string[]};
export function propertyMatch(client:ClientPreference,property:PropertyProfile){const checks:{label:string;done:boolean;weight:number}[]=[{label:"orçamento",done:(client.budgetMinCents===null||property.priceCents>=client.budgetMinCents)&&(client.budgetMaxCents===null||property.priceCents<=client.budgetMaxCents),weight:3},{label:"tipo",done:!client.desiredTypes.length||client.desiredTypes.some(v=>v.toLowerCase()===property.type.toLowerCase()),weight:2},{label:"região",done:!client.desiredRegions.length||client.desiredRegions.some(v=>[property.neighborhood,property.city].some(place=>place.toLowerCase().includes(v.toLowerCase()))),weight:3},{label:"quartos",done:client.minBedrooms===null||property.bedrooms>=client.minBedrooms,weight:1},{label:"banheiros",done:client.minBathrooms===null||property.bathrooms>=client.minBathrooms,weight:1},{label:"vagas",done:client.minParkingSpaces===null||property.parkingSpaces>=client.minParkingSpaces,weight:1},{label:"características",done:!client.desiredFeatures.length||client.desiredFeatures.every(v=>property.features.some(f=>f.toLowerCase().includes(v.toLowerCase()))),weight:2}];const total=checks.reduce((sum,c)=>sum+c.weight,0);const earned=checks.filter(c=>c.done).reduce((sum,c)=>sum+c.weight,0);return {score:Math.round(earned/total*100),reasons:checks.map(({label,done})=>({label,done}))};}
